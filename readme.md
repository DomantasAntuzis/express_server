# Useful commands

## Sequelize

1. migrate: npx sequelize-cli db:migrate
2. create new base model and migration: npx sequelize-cli model:generate --name "" --attributes name:string


!!! Do not forget to update migration files after updating models.